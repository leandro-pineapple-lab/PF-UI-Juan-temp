import { CanSaveSettingsDirective } from './can-save-settings.directive';

describe('CanSaveSettingsDirective', () => {
  it('should create an instance', () => {
    const directive = new CanSaveSettingsDirective();
    expect(directive).toBeTruthy();
  });
});
