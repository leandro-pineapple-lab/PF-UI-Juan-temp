import { SecurityAccessSettingsDirective } from './security-access-settings.directive';

describe('SecurityAccessSettingsDirective', () => {
  it('should create an instance', () => {
    const directive = new SecurityAccessSettingsDirective();
    expect(directive).toBeTruthy();
  });
});
