import { OrderByDirective } from './order-by.directive';

describe('OrderByDirective', () => {
  it('should create an instance', () => {
    const directive = new OrderByDirective();
    expect(directive).toBeTruthy();
  });
});
