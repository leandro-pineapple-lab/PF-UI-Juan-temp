import { PercentageSignPipe } from './percentage-sign.pipe';

describe('PercentageSignPipe', () => {
  it('create an instance', () => {
    const pipe = new PercentageSignPipe();
    expect(pipe).toBeTruthy();
  });
});
